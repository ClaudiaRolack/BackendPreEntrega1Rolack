<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{Carrito}}</title>
</head>
<body>
    
    <div>
        <p>Carrito ID: {{carts._id}}</p>
        <h2>Productos en el carrito:</h2>
        <ul>
            {{#each carts.products}}
            <li>
                <p>Nombre: {{this.productId.title}}</p>
                <p>Descripción: {{this.productsId.description}}</p>
                <p>Precio: {{this.productId.price}}</p>
                <p>Cantidad: {{this.quantity}}</p>
            </li>
            {{/each}}
        </ul>
    </div>

</body>
</html>